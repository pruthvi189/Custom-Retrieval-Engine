{
  "name": "vectordb-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "VectorDB - vector search algorithms (HNSW, KD-Tree, Brute Force) and grounded RAG, deployed as Vercel serverless functions",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node test/smoke.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
